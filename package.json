{
  "name": "notify-cli",
  "version": "1.0.0",
  "description": "Send notifications via macOS alerts and ntfy.sh push",
  "type": "module",
  "bin": {
    "notify-cli": "./bin/notify-cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
